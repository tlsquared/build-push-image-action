# Build and push image action

This repository contains an action for building and pushing Docker images in a
standardised way.

## Inputs

- `registry` - Server address of the registry
- `username` - Username used to log in
- `password` - Password used to log in
